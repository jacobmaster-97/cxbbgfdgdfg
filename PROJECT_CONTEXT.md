# 教師課堂工具：目前工作摘要

最後更新：2026-09-27

## 當前目標

把現有的「教師課堂工具」收斂為一個適合課室投影、平板與電腦使用的個人教師工作台。iPad 式主介面已承載既有工具，具備狀態列、工具庫側欄、可管理的底部捷徑與本機保存的顯示設定。

產品長期定位是教師日常工作的控制中心，而非堆疊 AI 內容生成器。第一優先仍是快速啟動的課堂工具與可共享的班別／學生資料；AI 留待可讀取既有資料後才加入。

## 已確認的關鍵決策

- 現階段為個人教師測試版，依最新指示在 GitHub 版本加入 Supabase 電郵密碼登入及私人資料庫。本機保存作快取，白板、圖片及功課紙不共用。多人協作與 AI 延後。
- 優先支援橫向大螢幕、電子白板、iPad、滑鼠、手指與觸控筆；課堂常用動作應在一至兩次點按內完成。
- 班別及學生已由班別管理、時間表及加分工具共用；分組、出席、功課與學生紀錄仍待接入。
- 網站以 `dist/` 作為靜態成品；不要把功能分散到新的獨立專案。
- 最新指示用回 GitHub 版本，以 origin/main 的 2944a15 為基礎；Apps Script 方案已封存。GitHub 靜態網站是本次目標，沒有推送 Sites。
- 每次更新完成並通過基本驗證後，可先提交至本機 Git；只有在使用者明確指示後，才可推送至 GitHub `origin/main`。

## 已交付的核心產出

- `dist/index.html`、`dist/app.js`、`dist/styles.css`：現有單頁教師工具儀表板。
- 電子白板：畫筆、螢光筆、擦膠、直線、顏色與粗幼、復原／重做、純白／黑板背景、多張本機白板、PNG 匯出、全螢幕與浮動倒數計時器。
- 功課紙：幼方格／大方格／橫線背景、建立多張本機功課紙、書寫、拖移、縮放及 PNG 匯出。
- 計時工具：考試時間投影與獨立倒數；可暫停、加時、縮小為浮窗並返回工具主頁。
- 教學遊戲：數學賓果及乘法寶藏，分別由 `dist/bingo.html` 與 `dist/multiplication-treasure.html` 提供。
- iPad 式工具殼層：狀態列、全螢幕、專注模式、工具庫側欄；可開啟所有工具，並將不常用工具移除／加回底部捷徑。
- 顯示設定：日落／海洋／森林主題、90–115% 介面比例、深色模式及減少動畫，均以本機保存。
- Git 儲存庫有 GitHub `origin` 與 Sites `deployment` remote。Supabase 功能已完成測試，使用者已確認本機預覽真實登入成功，並明確指示發布至 GitHub。

## 目前進度

| 範圍 | 狀態 |
| --- | --- |
| 白板、功課紙、計時與兩個遊戲 | 已可用，資料儲存在本機 |
| iPad 式應用程式殼層、工具庫與設定 | 已實作並以本機保存偏好 |
| 班別管理與學生名單 | 已實作，資料在同一帳戶跨裝置同步 |
| 加分／扣分紀錄 | 已實作，包括規則、批量加分、撤銷及植物進度；不同紀錄 ID 可安全合併 |
| 行事曆／時間表 | 已接入本機循環周課表、校曆、不適用日期與公眾假期 JSON；照片／PDF 來源可上載，等待辨認服務接入 |
| Supabase 登入及同步 | 已實作；實際專案公開金鑰有效、Email 啟用、註冊關閉、匿名 RPC 被拒；使用者已確認真實登入成功 |
| AI、多人協作 | 延後 |

## 未解決事項

- 「App Store」的第一版定位：現有工具庫已完成「開啟工具／管理捷徑」職能；不連接真實商店或下載服務。
- 相機與相簿是否需要進入第一版仍待確認；若加入，相簿只讀取使用者主動選擇的本機圖片，相機需在有明確課堂用途後才請求權限。
- Supabase 設定見 SUPABASE_SETUP.md；SQL 設定見 supabase/setup.sql。每個教師帳戶使用分开的本機快取與 RLS 保護的雲端工作區，整份快照 900 KB 上限，最近 50 版歷史。
- 班別、學生、時間表、校曆及加分紀錄納入共用；白板筆跡、個人圖片、功課紙、來源圖片／PDF與介面設定不納入，亦不保存其連結。
- 免費 Supabase 專案可能因一周低活動暫停；網站會保留本機修改並顯示連接錯誤，恢復專案後可重試。

## 下一步行動

1. 使用者已完成本機真實登入，發布後再於正式網址測試實際跨裝置讀寫；不要索取密碼。
2. 本次已獲明確 GitHub 發布指示；未來更新仍需依既定規則獲使用者指示才推送。
3. 先備份原網站本機資料，再於登入後明確匯入及發布；新帳戶不自動上傳舊名單。
4. 如確認課堂需要，再加入相簿或相機；不自行請求鏡頭權限。
